# Test Environment Manifest

Required lanes: Windows 11 clean VM; offline Windows lane; low/mid/high declared hardware; local llama.cpp; loopback MCP; corrupted/old database fixtures; accessibility keyboard/screen-reader lane; provider-native lanes only when explicitly authorized; GitHub throwaway test repository for repair PR workflow. Exact versions, hashes, CPU/RAM/GPU, network state, locale/timezone, and candidate artifact digest are captured per run rather than hard-coded here.
