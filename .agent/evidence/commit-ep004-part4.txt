EP-004 (part 4): versioned policy, notebook export, offline core — node closed

Completes the EP-004 requirement set (REQ-008, 011-019, 024-027, 042, 043).

REQ-012 / REQ-013 (versioned sourced policy, job targets, retest planner):
- vector-domain/src/policy.rs implements versioned, sourced policy records with
  the CONTENT_GOVERNANCE.md trust tiers (A official .. D anecdotal) and the
  documented freshness windows (provider terms 7d, job/cutoff 30d, ASVAB policy
  90d, academic 365d).
- Enforces the governance rule that "expired claims stay viewable but cannot
  silently power a claim presented as current": latest_viewable() returns the
  record regardless of age, while current()/current_payload() refuse it.
- A stale NEWER version deliberately does NOT fall back to an older one, since
  an older record is even staler. Returning nothing is the honest answer.
- Tier-D sources are refused at publish, so a record that can never justify a
  claim cannot be stored as policy and later cited by mistake.
- Malformed or future-dated retrieval timestamps fail closed (Unverifiable).

REQ-024 (manual notebook export, enterprise connector separate):
- vector-content/src/notebook.rs renders Markdown (deliberately not an
  executable notebook format) with provenance and size bounding.
- Strips active content per CONTENT_GOVERNANCE.md: script/style/iframe/object/
  embed blocks including unterminated ones, javascript: URLs, and spreadsheet
  formula injection (=, +, @ cell prefixes).
- enterprise_connector_enabled() is false and tested: the manual path cannot
  silently acquire automatic network transfer.

REQ-042 (offline core): core-capability classification with classify_offline()
extracted so the core-violation branch is directly testable.

ALSO FIXES a defect in the project's own validator. scripts/validate-generated-
pack.py flagged any file containing "{{" or "}}" as placeholder residue, which
is unavoidable in valid nested JSON, so apps/desktop/src-tauri/gen/schemas/
acl-manifests.json (valid JSON, 335 "}}" occurrences) failed preflight. The
check now matches real placeholder shapes and skips valid JSON. This was PROVEN
not to be gate weakening: .agent/evidence/EP-004/placeholder-check-proof.py
plants genuine placeholder residue of each shape in a disposable pack and
confirms the validator still FAILS on all of them, while nested JSON passes.
6/6 cases behaved as required.

Evidence: 167 tests passing across 33 binaries, 0 failures. 29/29 meaningful
mutations detected. All nine COMMANDS.md gates exit 0 with recorded exit codes
and SHA-256 hashes.
