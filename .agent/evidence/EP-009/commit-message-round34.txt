content: the local GGUF model, live (REQ-014)

REQ-014 read PARTIAL_TRANSPORT_REGISTERED_MODEL_NOT_LAUNCHED: the local_llama lane existed in the
transport registry and no model had ever been launched behind it. This round launches one and
proves the product sees it.

Launched: llama.cpp b11136 (MIT; llama-server.exe sha256 48927d3b0c2391bac96a181a3b55e55f286812f162b7dd42464a5c38188ff7b4)
serving qwen2.5-0.5b-instruct-q4_k_m.gguf from Qwen/Qwen2.5-0.5B-Instruct-GGUF, Apache-2.0 -- read
from the repository's own metadata through the Hugging Face API before the download, 491,400,032
bytes, sha256 74a4da8c9fbcd15bd1f6d01d621410d31c6fc00986f5eb687824e7b93d7a9db -- on loopback at
127.0.0.1:8099. Asked "In one sentence: what does an ohmmeter measure?", it answered "An ohmmeter
measures resistance in electrical circuits." in 0.67 s, read straight from the OpenAI-compatible
endpoint with its usage block, so the answer in the evidence is the model's and not a summary of it.

The product sees it. VECTOR_LLAMA_ENDPOINT=http://127.0.0.1:8099 vector-tools provider probe
--all-configured reports the local_llama lane healthy with the server up, and routes both local-only
and remote-allowed requests to it; with the server stopped it reports unavailable with the reason
("no llama.cpp server at http://127.0.0.1:8099 (connection timed out); set VECTOR_LLAMA_ENDPOINT to
a running llama.cpp server to use the local lane"). Both probe outputs are in the evidence
directory, because a health that is only ever observed as healthy is a constant, not a measurement.

A duplicate mechanism was deleted on the way. I first wrote a second local-probe module in
vector-llm with its own environment variable, then found probe_local_llama() already in
tools/vector-tools/src/transports.rs doing the same raw-TCP /health exchange against
VECTOR_LLAMA_ENDPOINT. The new module was removed and the existing path used: a second mechanism for
one question is drift, and the evidence for it would have been about my code rather than the product.

Two gate failures, both real, both fixed without weakening anything.
many_persisted_attempts_stay_within_budget failed during the full sweep: run alone it measures 1.08x
growth over 2,000 durable inserts, but inside the suite it tripped its 5x threshold because its first
250 inserts competed with nine sibling suites on the same disk and its last 250 did not. The
threshold was not the problem and lowering it would have been; the ten timing-sensitive tests in that
file now take a shared lock for the part they time, so the measurement gets the machine to itself.
And change-invalidation refused AI_TRANSPORTS.md as "a changed path no gate would notice", because
its surface table named COMMANDS.md and README.md individually; root documents now match by suffix --
and only at the root, so a .md file inside a crate stays what it is, source that happens to be prose.

REQ-014 moves to DONE_GGUF_MODEL_LIVEFIRED_TRANSPORT_HEALTHY and the open requirements fall from four
to three, all of them needing something this repository cannot supply: REQ-036 (a code-signing
certificate), REQ-038 (a person using a screen reader), REQ-060 (trademark clearance). Named in
AI_TRANSPORTS.md rather than implied: the lane has no screen for choosing the model file (the
endpoint is the configuration and the file is the server's argument) and no tutor surface sends a
lesson question through it yet. Routing already prefers it for both request classes.

DoD unchanged at 34 PASS / 7 PARTIAL / 1 EXTERNAL_REQUIRED; 21 gates recorded exit 0 after the two
fixes; verdict CONDITIONAL_EXTERNAL_GATES. Corpus unchanged at 6,961 active items; pack core-asvab v6
active.

Every in-repo requirement is now done. Next is the closure accounting that states exactly what
remains and who can close it.
